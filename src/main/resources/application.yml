server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mysql-0.sanchar.cn:3306/demo?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=yes
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath*:/mapper/*.xml
  type-aliases-package: per.san.domain

#showSql
logging:
  level:
    per.san.mapper: debug